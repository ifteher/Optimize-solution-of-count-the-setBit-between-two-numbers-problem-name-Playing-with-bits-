///Another optimize code:
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
        {
    int a,b,count=0;
    cin>>a>>b;
    for(int i=a;i<=b;i++){
        count+= __builtin_popcount(i);
    }
    cout<<count<<endl;
        }
    return 0;
}
